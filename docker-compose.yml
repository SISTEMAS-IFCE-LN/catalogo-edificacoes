networks:
  catalogo-net:
    driver: bridge
  catalogo-internal-net:
    driver: bridge

volumes:
  pgdata:
    name: pgdata

services:
  db:
    container_name: catalogo-db
    image: postgres:17-trixie
    user: postgres
    restart: unless-stopped
    volumes:
      - pgdata:/var/lib/postgresql/data
    environment:
      - TZ=America/Fortaleza
      - POSTGRES_DB=${DB_NAME}
      - POSTGRES_USER=${DB_USERNAME}
      - POSTGRES_PASSWORD=${DB_PASSWORD}
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U catalogo -d catalogo_edificacoes"]
      start_period: 1m
      interval: 5s
      timeout: 10s
      retries: 10
    networks:
      - catalogo-internal-net
    deploy:
      resources:
        limits:
          cpus: "1"
          memory: 2G

  api:
    container_name: catalogo-api
    build:
      context: ./apis
      dockerfile: Dockerfile
    restart: unless-stopped
    environment:
      - TZ=America/Fortaleza
      - PROFILE_ACTIVE=${PROFILE_ACTIVE}
      - DB_HOST=${DB_HOST}
      - DB_NAME=${DB_NAME}
      - DB_USERNAME=${DB_USERNAME}
      - DB_PASSWORD=${DB_PASSWORD}
      - GOOGLE_CLIENT_ID=${GOOGLE_CLIENT_ID}
      - GOOGLE_CLIENT_SECRET=${GOOGLE_CLIENT_SECRET}
      - BOOTSTRAP_ADMIN_EMAIL=${BOOTSTRAP_ADMIN_EMAIL}
      - JWT_PUBLIC_KEY_PATH=file:./.keys/public.pem
      - JWT_PRIVATE_KEY_PATH=file:./.keys/private_pkcs8.pem
      - JWT_COOKIE_SECURE=${JWT_COOKIE_SECURE}
      - JWT_COOKIE_SAME_SITE=${JWT_COOKIE_SAME_SITE}
      - FRONTEND_CALLBACK_SUCCESS_URL=${FRONTEND_CALLBACK_SUCCESS_URL}
      - FRONTEND_CALLBACK_ERROR_URL=${FRONTEND_CALLBACK_ERROR_URL}
      - CORS_ALLOWED_ORIGINS=${CORS_ALLOWED_ORIGINS}
    volumes:
      - ./apis/.keys:/app/.keys:ro
    healthcheck:
      test: ["CMD", "java", "-cp", "/app", "HealthCheck"]
      start_period: 2m
      interval: 10s
      timeout: 5s
      retries: 5
    depends_on:
      db:
        condition: service_healthy
    networks:
      - catalogo-net
      - catalogo-internal-net
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: 1G

  frontend:
    container_name: catalogo-frontend
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        # Origem do backend embutida no bundle (vazio = mesma origem via Nginx)
        VITE_BACKEND_URL: ${BACKEND_URL}
    restart: unless-stopped
    environment:
      - TZ=America/Fortaleza
    ports:
      - "8000:8000"
    depends_on:
      api:
        condition: service_healthy
    networks:
      - catalogo-net
    deploy:
      resources:
        limits:
          cpus: "0.25"
          memory: 256M
